<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="ROBOTS" content="NOODP" />
    <meta name="viewport" content="initial-scale=1" />
    <title>Certificate | ASI Rewards</title>

    <!-- favicon incon -->
    <%= favicon_link_tag "favicon.png", rel: "icon", type: "image/png" %>

    <%= stylesheet_link_tag :certificate_editor %>
    <%= csrf_meta_tag %>
  </head>

  <body class="<%= body_class %>">
    <div class="container certificate">
      <%= yield %>
    </div>

    <!-- javascript -->
    <%= javascript_include_tag :certificate_editor %>
  </body>
</html>
