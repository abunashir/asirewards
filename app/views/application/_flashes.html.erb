<% if flash.any? %>
  <div class="flashes">
    <% user_facing_flashes.each do |key, value| -%>
      <div class="alert alert-<%= key_to_bootstrap_class(key)  %>" role="alert">
        <%= value %>
      </div>
    <% end -%>
  </div>
<% end %>
